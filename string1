#include <bits/stdc++.h>
using namespace std;
int main(){
    const int n = 50;
    char sent[n];
    int i = 0;
    cout << "輸入一段英文句子: ";
    cin.getline(sent, n);
    while (sent[i])
        cout << char(toupper(sent[i++]));
    cout << endl;
    
}
